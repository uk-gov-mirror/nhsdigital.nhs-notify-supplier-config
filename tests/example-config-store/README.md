# Example config store

This directory contains a minimal sample supplier config store for local CLI testing.

Persisted entity folders included:

- `volume-group`
- `letter-variant`
- `pack-specification`
- `supplier`
- `supplier-allocation`
- `supplier-pack`
