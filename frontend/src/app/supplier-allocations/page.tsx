import { PlaceholderPage } from "@/components/placeholder-page";

export default function SupplierAllocationsPage() {
  return (
    <PlaceholderPage
      title="Supplier allocations"
      description="This area will manage percentage allocations across suppliers and volume groups."
    />
  );
}
